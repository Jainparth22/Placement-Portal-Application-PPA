Flask>=3.0
Flask-SQLAlchemy>=3.1
Flask-CORS>=4.0
Flask-Mail>=0.10
Werkzeug>=3.0
SQLAlchemy>=2.0.36

# Authentication — custom JWT implementation (PyJWT)
# Flask-Security was evaluated; custom implementation chosen for full control
# over token payload, role decorators, and blacklist logic. See auth.py.
PyJWT>=2.8

# Background jobs & caching
redis>=5.0
celery>=5.4

# Environment & config
python-dotenv>=1.0

# Resume processing
PyPDF2>=3.0

# ATS check (Hugging Face Gradio)
gradio_client>=1.0

# Report generation
xhtml2pdf>=0.2.11

# HTTP requests (for webhook notifications)
requests>=2.31

# Production WSGI server
gunicorn>=21.2
